#!/bin/sh

set -e

if [ "$1" = "remove" ] || [ "$1" = "disappear" ] ; then
	update-alternatives --remove pulpdoctest /usr/bin/python2-pulpdoctest
	update-alternatives --remove pulptest /usr/bin/python2-pulptest
fi

#DEBHELPER#

exit 0
